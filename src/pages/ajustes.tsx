import type { NextPage } from "next";
import { Layout } from "../components/template/Layout";

const Ajustes: NextPage = () => {
  return (
    <Layout titulo="Ajustes" subtitulo="Ajustes">
      <h3>Conteúdo</h3>
    </Layout>
  );
};

export default Ajustes;
